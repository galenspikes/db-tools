# MySQL DB Rename Utility

Use this to quickly rename a MySQL database schema (and all its dependent objects)

Confluence: https://ncct-confluence.epa.gov/display/SYS/Renaming+Schemas

## Usage

```bash
mysql_db_rename oldSchemaName newSchemaName mysqlLoginPath
```

